# The MVP 0.1 pipeline needs no API keys: Midjourney generation is manual
# (PRD section 5/12), and the review UI runs entirely on local files/SQLite.
# This file is a placeholder for future provider credentials (PRD section 46)
# — never commit a real .env.

BOOKFACTORY_HOST=127.0.0.1
BOOKFACTORY_PORT=8000

# Reserved for a future API-based ImageGenerator provider.
# IMAGE_PROVIDER_API_KEY=
